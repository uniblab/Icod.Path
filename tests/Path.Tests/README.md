# Icod.Path.Tests

This project validates Completion Gate E2 independently of future command behavior. Synthetic providers exercise POSIX and Windows grammar on every runner, while host tests conditionally verify real symbolic links, dangling links, and loops where the operating system permits their creation.
